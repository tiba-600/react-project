'use strict';

/**
 * contact-formulier service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-formulier.contact-formulier');
